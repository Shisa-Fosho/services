Canonical rules live in `docs/rules/polymarket.md` — read that file before
making changes to this codebase. This stub exists so the Claude Code / Codex
harness auto-discovers the project rules at session start; the actual content
is centralized in `docs/rules/` so edits only happen in one place.
