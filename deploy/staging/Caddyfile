:8443 {
  tls /etc/caddy/certs/tailscale.crt /etc/caddy/certs/tailscale.key
  encode gzip

  header {
    Strict-Transport-Security "max-age=31536000"
    X-Content-Type-Options "nosniff"
  }

  reverse_proxy trading:8080
}

:8444 {
  tls /etc/caddy/certs/tailscale.crt /etc/caddy/certs/tailscale.key
  encode gzip

  header {
    Strict-Transport-Security "max-age=31536000"
    X-Content-Type-Options "nosniff"
  }

  reverse_proxy platform:8081
}
